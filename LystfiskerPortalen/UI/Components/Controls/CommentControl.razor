@using LystfiskerPortalen.UI.ViewModels

<div class="border rounded p-2 mb-2">
    <div class="fw-bold">@Comment.AuthorName</div>
    <div>@Comment.Content</div>
    <div class="text-muted small">@Comment.CreatedAt.ToString("g")</div>
</div>

@code {
    [Parameter] public InteractionViewModel Comment { get; set; }
}